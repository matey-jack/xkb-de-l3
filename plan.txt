# plan iteration

Since we need a reboot for every test, we can implement the whole thing as full draft version in one go.

xkb data model entities:
 - keyboard 'model' (physical shape, number of keys and their names)
 - 'layout' (actually: keymap): the language/culture/country definition
 - keymap 'variant'
 - keymap 'option'

The old cozy-keyboard was implemented as a variant of German ('de') and included all changes:
letter map, wide-hands, modifiers, and higher layers.

decisions:
 - should CAPS as SHIFT (including Shift+Shift as CAPS) be implemented as an option? - YES!
 - should LSGT as AltGr (including AltGr mappings for <>|) be implemented as a separate option?
 - The remaining changes affect only AltGr (incl Shift+AltGr) and the single character §.
   They can all live in a single 'variant' - and the name is 'de-coding' ?

tasks:
- create new keymap name in symbols/de and add it to rules/{evdev,base}.{xml,lst}

# specification

This is inspired from https://github.com/matey-jack/keyboard-layer3

But with some of the AltGr mappings taking from https://github.com/matey-jack/qmk_userspace_iris_cozy_keymap


modifiers:
* CAPS as Shift
* Shift+Shift as CAPS
* <> as AltGr

changed Shift layer:
 * Shift+3 = @, replacing §.

AltGr (and AltGr+Shift) layers left:

^  ¡ #¤ §£ €¢  ‰
      UP
 |< LF DN RT >|    

The nav keys are mapped to both AltGr and Shift+AltGr layers just to avoid any misunderstandings.
The lower left row stays unchanged except for AltGr+B (and Shift+AltGr+B) acting as a delete key.
Its distance from the nav keys is to avoid accidental presses.

AltGr layer right:


^  |  [  ]  }  \¿  `
    \  {  }  ~
 `  /  (  )  ;
  ×  =µ <  >  –
  
Notes:

 * characters ` and ^ are "live" keys used for programming characters, since the original DE keymap has them as dead keys on the base and Shift layers. We leave them there and add duplicates on the AltGr layer in the same positions as the Iris keymap linked to above. 

 * characters & and | don't fall on the Shift and AltGr layer of the same key here (as they do on the Iris), because the DE keymap has & further to the left and I don't change that here. 

 * Only four rarely used characters are on the AltGr+Shift layer. The pair ¿¡ is already mapped there in the xkb DE standard, otherwise I would move them to plain AltGr. 

 * Some keys are mapped twice, so that the old position still works in case ancient muscle memory uses it.

 * I'll probably make some tweaks to the plan as I have a further look on the standard xbk DE to minimize changes and preserve some of its useful mappings.


